% Book class with custom preamble
\usepackage{graphicx}
\usepackage{helvet} % For Helvetica font
% \renewcommand{\rmdefault}{serif} % Serif font for body
% \renewcommand{\sfdefault}{phv} % Sans-serif Helvetica for title and page numbers
\usepackage{geometry}
\usepackage{lipsum} % For sample text
\usepackage{fancyhdr}
\usepackage{marginnote} % For margin notes

% Geometry setup for margins
\geometry{
  left=100mm, 
  right=40mm,
  top=25mm,
  bottom=40mm,
  marginparwidth=40mm,
  marginparsep=25mm,
}
\reversemarginpar % This moves the margin notes to the left

% Redefine marginnote to be left aligned
\renewcommand{\marginnote}[1]{
    \ifhmode\leavevmode\fi
    \begin{flushleft}
    \marginpar{\raggedright #1}
    \end{flushleft}
}

% % Custom settings for the recipe title and page numbers
% \titleformat{\chapter}[hang] % Customize the chapter title (recipe title)
%   {\Huge\bfseries\sffamily} % Large Helvetica sans-serif font
%   {} % No chapter number
%   {0pt} % No spacing between the title and chapter number
%   {\Huge\raggedright\hspace{-1in}} % Shift the title 1 inch to the left

% Disable "Chapter X" from appearing
\makeatletter
\renewcommand{\@makechapterhead}[1]{%
  {\parindent \z@ \raggedright \normalfont
    \interlinepenalty\@M
    \Huge \bfseries \sffamily \hspace{-50mm}#1\par\nobreak
    \vskip 100\p@}
    \thispagestyle{fancy}  % add page number
  }
\makeatother


% Header/Footer setup
\fancyhf{} % Clear existing header/footer
\fancyfoot[L]{\Huge\textsf{\hspace{-50mm}\thepage}} % Large page number moved 1 inch left
% \fancyfoot[C]{\large\textsf{\partmark}} % Center the part title (e.g., "Appetizers") without "Part 1"
\renewcommand{\headrulewidth}{0pt} % No header rule
\renewcommand{\footrulewidth}{0pt} % No footer rule
\pagestyle{fancy}

